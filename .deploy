cd buildchain
npm ci
npm run build
cd ..

cd cms
composer install --no-scripts --no-interaction --prefer-dist --optimize-autoloader --ignore-platform-reqs
cd ..

git add -A
Echo "Add a commit message for this deployment:"
read commitmessage
git commit -m \'$commitmessage\'
git push
